---
title: "GLM-4-AirX"
description: "GLM-4-AirX 是 GLM-4-Air 的高性能版本 ，继承 GLM-4 基座模型的多语言理解与复杂推理能力的同时，具备超快的推理速度和强大的推理效果。"
---

<CardGroup cols={3}>
  <Card title="价格" icon="circle-dollar">
    10 元 / 百万Tokens
  </Card>
  <Card title="输入模态" icon="arrow-down-right">
    文本
  </Card>
  <Card title="输出模态" icon="arrow-down-left">
    文本
  </Card>
  <Card title="上下文窗口" icon="arrow-down-arrow-up" iconType="regular">
    8K
  </Card>
  <Card title="最大输出Tokens" icon="maximize" iconType="regular">
    4K
  </Card>
</CardGroup>

## <Icon icon="stars" iconType="solid" color="#3890ff" size={28} />   推荐场景

<AccordionGroup>
  <Accordion title="智能客服系统" icon="robot">
    可同时承接海量用户咨询，毫秒级响应客户关于产品功能、订单状态、售后政策等问题，精准理解多语言咨询内容，快速调用企业业务知识库生成专业解答，有效提升客户服务效率与满意度。
  </Accordion>
  <Accordion title="实时数据分析" icon="database">
    面对金融交易数据、工业传感器数据等高频实时数据流，能在高并发场景下实现低延迟推理分析，迅速识别数据异常、预测趋势变化，并结合行业知识生成可落地的决策建议。（推荐与[搜索工具](https://www.bigmodel.cn/dev/howuse/websearch)结合，可获取更新资讯，回答结果更加实时、准确）
  </Accordion>
  <Accordion title="数学问题推理" icon="chart-scatter-3d">
    在在线数学教育平台或科研辅助场景中，可同时处理高并发的数学问题求解请求，毫秒级解析代数证明、几何建模、微积分推导等复杂数学问题。
  </Accordion>
</AccordionGroup>

## <Icon icon="bars-sort" iconType="solid" color="#3890ff" size={28} />   使用资源

[体验中心](https://bigmodel.cn/trialcenter/modeltrial/text?modelCode=glm-4-airx)：快速测试模型在业务场景上的效果\
[接口文档](https://bigmodel.cn/dev/api/normal-model/glm-4)：API调用方式

## <Icon icon="arrow-down-from-line" iconType="solid" color="#3890ff" size={28} />   详细介绍

<Steps>
  <Step icon="star">
    GLM-4-AirX 专为低延时、高并发场景设计，利用 15T 高质量数据进行预训练，特别纳入了丰富的推理类合成数据，为后续的强化学习扩展奠定了基础。在后训练阶段，除了进行面向对话场景的人类偏好对齐，我们还通过拒绝采样和强化学习等技术，重点增强了模型在指令遵循、工程代码生成、函数调用等任务上的表现，以强化智能体任务所需的原子能力。

    该模型在保障与 GLM-4-Air-250414 的同等性能外，还进行了模型基础组件的技术迭代，推理环节中包含了prefill和decoder的自回归输出两个阶段，使得 GLM-4-AirX 获得更快推理速度和更强大的推理能力。
  </Step>
</Steps>

## <Icon icon="arrow-down-big-small" iconType="solid" color="#3890ff" size={28} />   功能支持

<CardGroup cols={3}>
  <Card title="流式输出" icon="arrow-down-from-dotted-line">
    
  </Card>
  <Card title="结构化输出" icon="arrow-down-from-line">
    
  </Card>
  <Card title="函数调用" icon="function">
    
  </Card>
</CardGroup>

## <Icon icon="rectangle-code" iconType="solid" color="#3890ff" size={28} />   调用示例

<Tabs>
  <Tab title="输入示例">
    ```python
    from zhipuai import ZhipuAI
    client = ZhipuAI(api_key="")  # 请填写您自己的APIKey
    response = client.chat.completions.create(
    model="glm-4-airx",  # 请填写您要调用的模型名称
    messages=[
    {"role": "user", "content": "结合概率统计与线性代数：计算二维随机变量的协方差矩阵，并判断矩阵正定性"},
    ],
    )
    print(response)
    ```
  </Tab>
  <Tab title="输出示例">
    ```response
    Completion(model='glm-4-airx', created=1749095539,
    choices=[CompletionChoice(index=0, finish_reason='stop',
    message=CompletionMessage(content='要计算二维随机变量 \\(X\\) 和 \\(Y\\) 的协方差矩阵，
    我们首先需要知道这两个随机变量的协方差 \\( \\text{Cov}(X, Y) \\) 和各自的方差 \\( \\text{Var}(X)
    \\) 和 \\( \\text{Var}(Y) \\)。协方差矩阵是一个 \\(2 \\times 2\\) 的矩阵，其形式如下：\n\n\\[\n
    \\begin{bmatrix}\n\\text{Var}(X) & \\text{Cov}(X, Y) \\\\\n\\text{Cov}(Y, X) & \\text{Var}(
    Y)\n\\end{bmatrix}\n\\]\n\n其中，由于协方差是对称的，即 \\( \\text{Cov}(X, Y) = \\text{Cov}(Y, X
    ) \\)。\n\n以下是计算协方差矩阵的一般步骤：\n\n1. **计算 \\(X\\) 和 \\(Y\\) 的期望值 \\(E(X)\\)
    和 \\(E(Y)\\)**：\n   \\[\n   E(X) = \\sum_{i=1}^{n} x_i P(x_i)\n   \\]\n   \\[\n
    E(Y) = \\sum_{i=1}^{n} y_i P(y_i)\n   \\]\n   其中，\\(P(x_i)\\) 和 \\(P(y_i)\\)
    分别是 \\(X\\) 和 \\(Y\\) 的概率分布。\n\n2. **计算 \\(X\\) 和 \\(Y\\) 的方差
    \\( \\text{Var}(X) \\) 和 \\( \\text{Var}(Y) \\)**：\n   \\[\n   \\text{Var}
    (X) = E(X^2) - (E(X))^2\n   \\]\n   \\[\n   \\text{Var}(Y) = E(Y^2) - (E(Y))^2\n   \\]\n
    其中，\\(E(X^2)\\) 和 \\(E(Y^2)\\) 分别是 \\(X^2\\) 和 \\(Y^2\\) 的期望值。
    \n\n3. **计算 \\(X\\) 和 \\(Y\\) 的协方差 \\( \\text{Cov}(X, Y) \\)**：\n
    \\[\n   \\text{Cov}(X, Y) = E(XY) - E(X)E(Y)\n   \\]\n   其中，\\(E(XY)\\) 是
    \\(X\\) 和 \\(Y\\) 的乘积的期望值。\n\n4. **将这些值填入协方差矩阵**：\n   \\[\n   \\text
    {协方差矩阵} = \\begin{bmatrix}\n   \\text{Var}(X) & \\text{Cov}(X, Y) \\\\\n
    \\text{Cov}(Y, X) & \\text{Var}(Y)\n   \\end{bmatrix}\n   \\]\n\n接下来，判断矩阵的正定性：
    \n\n一个 \\(2 \\times 2\\) 的矩阵 \\(A\\) 是正定的，当且仅当它的所有主子式都是正的。对于 \\(2 \
    \times 2\\) 的协方差矩阵，我们只需要检查以下两个条件：\n\n1. **矩阵的对角线元素（即方差）必须都是正的*
    *：\n   \\[\n   \\text{Var}(X) > 0\n   \\]\n   \\[\n   \\text{Var}(Y) > 0\n   \\]\n\n2. *
    *矩阵的行列式必须也是正的**：\n   \\[\n   \\text{det}(\\text{协方差矩阵}) = \\text{Var}(X)
    \\cdot \\text{Var}(Y) - \\text{Cov}(X, Y)^2 > 0\n   \\]\n\n如果这两个条件都满足，那么协方差
    矩阵是正定的。这意味着随机变量 \\(X\\) 和 \\(Y\\) 是正相关的，并且它们各自具有非零的方差。',
    role='assistant', tool_calls=None))],
    request_id='2025060511521018031186965b4072',
    id='2025060511521018031186965b4072',
    usage=CompletionUsage(prompt_tokens=26, completion_tokens=786, total_tokens=812))
    ```
  </Tab>
</Tabs>

## <Icon icon="square-user" iconType="solid" color="#3890ff" size={28} />   用户并发权益

API调用会受到速率限制，当前我们限制的维度是请求并发数量（在途请求任务数量）。不同等级的用户并发保障如下。

| V0 | V1 | V2 | V3 |
| --- | --- | --- | --- |
| 5  | 30 | 40 | 50 |